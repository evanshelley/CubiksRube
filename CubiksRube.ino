#include "CubiksRube.h"
#include "PhysicalCube.h"

void setup() {
  // setup
    // Scan into virtual rep via PhysicalCube functions
      // Get return vals of scan from physical cube
      // Take values and put into our virtual representation
}

void loop() {
  // Get action (algo)
  // Do action
}

// Need a method and means to convert rubik notation into virtual rep transform
// e.g. [1, F] ==> F transform on face 1
// e.g. [5, F'] ==> F' transform on face 5
